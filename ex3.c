#include <stdio.h>

int main(int argc, char const *argv[]) {
    int age = 22;
    int height = 72;
    printf("I am %d years old.\n", age);
    printf("I am %d inches tall.\n", height);
    return 0;
}

/* %d formated string, will be placed by the variable later,
construct a new string, and then printing it to the terminal */
